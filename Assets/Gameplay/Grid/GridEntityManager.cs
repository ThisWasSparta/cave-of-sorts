using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GridEntityManager : MonoBehaviour {


	public void CreateEntityOnGrid() {
		// create the entity on an empty grid position, inform the stack that it's blocked
		// create the tile the entity has to move to
	}

	public void Move() {
		// move to the next tile, spin in place if boxed in
		// spin in place when attempting to put a tile on its position
		//

		// pathfinding?
		// check if reached position, if so, let the objective tutorialManager know
	}
}
